[project]
name = "ansible-role-simple-compose"
version = "0.1.0"
description = ""
authors = [{ name = "Tim Friedrich Brüggemann", email = "oss@timfb.dev" }]
requires-python = "~=3.14.7"
readme = "README.md"
dependencies = ["ansible>=14.2,<15"]

[dependency-groups]
dev = [
	"ansible-dev-tools>=26.7.2,<27",
	"molecule-plugins[docker]>=26.7.15,<27",
	"ansible-lint>=26.6,<27; platform_system != 'Windows'",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
